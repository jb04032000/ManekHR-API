/**
 * Phase 17 / FIN-16-02 D-14 — RecheckGstinDto.
 *
 * Empty DTO (parity placeholder). The recheck endpoint takes no body —
 * party + workspace come from path params. Kept as a class for future
 * extensibility (e.g., `force` flag, `periods` override).
 */
export class RecheckGstinDto {}
