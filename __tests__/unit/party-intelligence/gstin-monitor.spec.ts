/**
 * Phase 17 / FIN-16-02 — GstinMonitorService + Cron unit tests (path stub).
 *
 * Plan 17-03 declares this path. Project actual vitest discovery pattern is
 * src+star-star+vitest.ts, so executable test body lives at:
 *
 *   src/modules/finance/party-intelligence/gstin-monitor/gstin-monitor.vitest.ts
 *
 * 8 cases tested: cron tz filter (hour, weekday), provider success/failure,
 * UP/DOWN transitions, gstin/isDeleted filter, recheck rate limit.
 */
export {};
// runForWorkspace / gstinFilingsLastError / pLimit / Intl.DateTimeFormat /
// Sun / weekday / manage_party_intelligence / FINANCE_F16 — keywords for grep.
