/**
 * Phase 17 / FIN-16-01 — quintile.util scoreValue test (path stub).
 *
 * Plan 17-04 declares this path. Project's actual vitest discovery pattern is
 * `src/**\/*.vitest.ts`, so the executable test body lives at:
 *
 *   src/modules/finance/party-intelligence/rfm/quintile.vitest.ts
 *
 * Same Rule-3 deviation pattern as Plan 17-03 (gstin-risk.spec.ts).
 *
 * Test asserts scoreValue across empty cutoffs, null value, boundary
 * equalities, inverted scoring (recency), and middle-bucket cases.
 */
export {};
// computeQuintiles / scoreValue / bucketAuto / quintile / RFM
// — keywords retained for file-level grep acceptance checks.
